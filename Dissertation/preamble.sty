% imports
\usepackage[pdfborder={0 0 0}]{hyperref}  % turns references into hyperlinks
\usepackage{graphicx}  % allows inclusion of PDF, PNG and JPG images
\usepackage{fancyhdr}  % for tweaking the running header/footer lines
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[utf8]{inputenc}
\usepackage{titlesec}
\usepackage{prooftree}
\usepackage{stmaryrd}
\usepackage{minted}
\usepackage{scalerel}
\usepackage{tikz}
\usepackage{svg}
\usepackage{booktabs}
\usepackage{newunicodechar}
\usepackage[scr=rsfs]{mathalpha}
\usepackage{textgreek}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{multicol}
\usepackage{tombstone}
\usepackage{float}
\usepackage{tablefootnote}
\usepackage{amsthm}
\usepackage{cancel}
\usepackage{algorithm2e}
\usepackage{changepage}
\usepackage{stackengine}
\usepackage{xcolor}
\usepackage{pgfplots}
\usepackage{enumitem}
\usepackage{nccmath}

% Add extra components to graphics library
\usetikzlibrary{shapes,positioning,arrows,patterns}

% Map unicode characters to latex 
% To imbed latex symbols into minted code snippets 
% E.g. ➔ in a minted code snippet is represented as \rightarrow
\newunicodechar{′}{\(^\boldsymbol{\prime})}
\newunicodechar{₁}{\({}_{\boldsymbol{1}}\)}
\newunicodechar{₂}{\({}_{\boldsymbol{2}}\)}
\newunicodechar{∪}{\( \cup \)}
\newunicodechar{➔}{\(\boldsymbol{\rightarrow}\)}
\newunicodechar{🠔}{\(\boldsymbol{\leftarrow}\)}
\newunicodechar{Ǵ}{\(\,\Gamma\;\)}
\newunicodechar{Ğ}{\(\,\Gamma_\emph{start}\;\)}
\newunicodechar{ǧ}{\(\,\Gamma_\emph{end}\;\)}
\newunicodechar{│}{\(\boldsymbol{|}\)}
\newunicodechar{ï}{\(\,\Gamma_\emph{if}\;\)}
\newunicodechar{ë}{\(\,\Gamma_\emph{else}\;\)}
\newunicodechar{í}{\(\,\Gamma_\emph{if-strip}\;\)}
\newunicodechar{é}{\(\,\Gamma_\emph{else-strip}\;\)}
\newunicodechar{∙}{\(\,\boldsymbol{\cdot}\)}
\newunicodechar{𝝉}{\(\tau\)}
\newunicodechar{↦}{\(\mapsto\)}
\newunicodechar{µ}{\(\mbox{\(\mu\)\hspace{0.05mm}\llap{\(\mu\)}}\)}

% page layout
\usepackage[margin=2cm]{geometry}
\setlength{\parindent}{0pt}
\setlength{\parskip}{\medskipamount}
% \renewcommand{\baselinestretch}{1.1}

% title spacing
\titleformat{\chapter}[display]
{\normalfont\huge\bfseries}{\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titlespacing*{\chapter}{0pt}{0pt}{10pt}

% don't let large figures hijack entire pages
\renewcommand\topfraction{.9}
\renewcommand\textfraction{.1}
\renewcommand\floatpagefraction{.8}

% get rid of empty pages
\renewcommand{\cleardoublepage}{\clearpage}

\raggedbottom
\sloppy
\clubpenalty1000%
\widowpenalty1000%
